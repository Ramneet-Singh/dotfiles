# Brewfile — declarative list of formulae & casks for `brew bundle`.
# Install with: brew bundle --file=Brewfile  (or just: ./brew.sh)

# --- Build / dev tools ---
brew "automake"
brew "clang-format"
brew "cmake"
brew "coreutils"
brew "ninja"

# --- VCS & code search ---
brew "git"
brew "git-filter-repo"
brew "gh"
brew "ack"
brew "tree"
brew "wget"

# --- Shell experience ---
brew "fzf"                       # fuzzy finder (sourced from ~/.fzf.zsh)
brew "powerlevel10k"             # zsh prompt theme (sourced by .zshrc)
brew "zsh-syntax-highlighting"   # sourced from /opt/homebrew/share in .zshrc
brew "zsh-autosuggestions"       # sourced from /opt/homebrew/share in .zshrc

# --- Editors ---
brew "macvim"

# --- Languages / runtimes ---
# Node is installed via nvm (see bootstrap.sh) for per-project version switching.
brew "python@3.12"               # primary Python

# --- Compilers / libraries (uncomment if needed for current projects) ---
# brew "ffmpeg"
# brew "graphviz"
# brew "libomp"
# brew "diff-pdf"

# --- Casks (GUI apps + fonts) ---
cask "iterm2"
cask "miniconda"
cask "font-meslo-lg-nerd-font"   # needed for Powerlevel10k icons
cask "visual-studio-code"
cask "spotify"
